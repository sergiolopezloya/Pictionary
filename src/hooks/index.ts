/**
 * Central export file for all custom hooks
 * Provides clean imports throughout the application
 */

// Animation hooks
export { useRiveAnimation } from './useRiveAnimation';

// Game state hooks
export { useGameState } from './useGameState';

// Hook types are defined inline in the hook files for better encapsulation
